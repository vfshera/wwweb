pre-commit:
  commands:
    format:
      glob: "**/*.{ts,tsx}"
      run: bunx run oxfmt --no-error-on-unmatched-pattern {staged_files}

    lint:
      glob: "**/*.{ts,tsx}"
      run: bunx run oxlint --fix {staged_files}
